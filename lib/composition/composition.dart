import 'package:flutter/material.dart';

class Composition extends StatelessWidget {
  const Composition({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Composition'),);
  }
}
