import 'package:flutter/material.dart';

class Venues extends StatelessWidget {
  const Venues({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Text('Venues'),);
  }
}